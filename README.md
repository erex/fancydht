Stratified abundance/density estimation
=======================================

This deals with the case of more complex post-stratification for detection function models fitted by `Distance` and `mrds`.

Currently a work in progress.
